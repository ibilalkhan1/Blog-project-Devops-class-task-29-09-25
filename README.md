# DevOps Full-Stack Blog (Node + React + Postgres + Docker Compose)
Run locally:
1) cp .env.example .env
2) docker compose up --build
Frontend ➜ http://localhost:5173
API ➜ http://localhost:4000/healthz and /api/posts
